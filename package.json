{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Task planner frontend",
  "scripts": {
    "dev": "node dev-server.js",
    "build": "node build.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "esbuild": "^0.28.0",
    "tailwindcss": "^4.2.2"
  }
}
